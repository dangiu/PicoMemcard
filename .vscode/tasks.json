{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake --build ${workspaceRoot}/build --config Debug --target all -j 10",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}